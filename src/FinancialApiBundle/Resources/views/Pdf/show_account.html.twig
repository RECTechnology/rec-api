{% block show_account %}
    <strong>{{ "Name" | trans }}:</strong>
    <em>{{ account.name }}</em><br/>
    <strong>{{ "Phone" | trans }}:</strong>
    <em>+{{ account.prefix }}
        {{ account.phone }}</em><br/>
    {% if account.email %}
        <strong>{{ "Email" | trans }}:</strong>
        <em>{{ account.email }}</em><br/>
    {% endif %}
    {% if (account.street != null) or (account.addressNumber != null) or (account.city != null) or ( account.zip != null) %}
        <strong>{{ "Address" | trans }}:</strong>
        <em>
            {{ account.streetType }}
            {{ account.street }}
            {{ account.addressNumber }},
            {{ account.city }}
            {{ account.zip }}
            {% if account.neighbourhood %}
                ({{ account.neighbourhood.name }})
            {% endif %}
        </em>
    {% endif %}
{% endblock %}
