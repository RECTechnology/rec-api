
<html>
<head>
    <title>NFC {{ action }}</title>
    <meta charset="UTF-8">

    <style>

        @import url(https://fonts.googleapis.com/css?family=Roboto:400,300,100);
        h1,h2,h3,h4,h5,h6,p,a{
            font-family: 'Roboto', sans-serif;
        }
        body{
            margin: 0;
            padding: 0;
        }
        header{
            width: 700px;
            margin: 0 auto;
            padding: 60px 0 20px;
            /* Permalink - use to edit and share this gradient: http://colorzilla.com/gradient-editor/#218fec+48,1976cb+100 */
            background: #218fec; /* Old browsers */
            background: -moz-radial-gradient(center, ellipse cover,  #218fec 48%, #1976cb 100%); /* FF3.6-15 */
            background: -webkit-radial-gradient(center, ellipse cover,  #218fec 48%,#1976cb 100%); /* Chrome10-25,Safari5.1-6 */
            background: radial-gradient(ellipse at center,  #218fec 48%,#1976cb 100%); /* W3C, IE10+, FF16+, Chrome26+, Opera12+, Safari7+ */
            filter: progid:DXImageTransform.Microsoft.gradient( startColorstr='#218fec', endColorstr='#1976cb',GradientType=1 ); /* IE6-9 fallback on horizontal gradient */
        }
        header img{
            width: 50px;
            display: block;
            margin: 0 auto;
        }
        h1{
            font-weight: lighter;
            text-align: center;
            color: #fff;
            margin: 5px 0;
        }
        article{
            width: 650px;
            margin: 0 auto;
            padding: 15px 0;
        }
        article p{
            color: #4D4D4D;
            font-size: 13px;
            text-align: justify;
        }
        article div{
            text-align: center;
            margin: 40px 0;
        }
        article div p{
            text-align: center;
        }
        article a{
            color: #218FEC;
        }

        footer{
            background: #4D4D4D;
            width: 700px;
            margin: 0 auto;
            padding: 20px 0;
        }
        footer a{
            color: #fff;
            text-decoration: none;
            text-align: center;
            display: block;
            font-weight: lighter;
        }

    </style>

</head>
<body>
<header>
    <img src="https://chip-chap.com/images/chip-chap_white.png">
    <h1>{{ action }}</h1>
</header>
<article>
    EN
    {% if action == 'refresh_pin' %}
        <p>We have received a new request to change your NFC Card pin <b>{{ card.alias }}</b></p>
        <p>Your new pin is {{ card.newPin }}</p>
        <p>To confirm this change please follow the link below</p>
        <a href="{{ url }}{{ card.refreshPinToken }}">{{ card.alias }}</a>
        <p>If you have not requested this change ignore this message</p>
    {% elseif action == 'disable' %}
        <p>Disable one of this cards:</p>
        <ul>
            {% for card in cards %}
                <li><a href="{{ url }}{{ card.confirmationToken }}">{{ card.alias }}</a></li>
            {% endfor %}
        </ul>
    {% endif %}
    ES
    {% if action == 'refresh_pin' %}
        <p>Hemos recibido una solicitud para cambiar el pin de la NFC Card <b>{{ card.alias }}</b></p>
        <p>Tu nuevo pin es {{ card.newPin }}</p>
        <p>Para confirmar el cambio por favor sigue este link</p>
        <a href="{{ url }}{{ card.refreshPinToken }}">{{ card.alias }}</a>
        <p>Si tu no has solicitado este cambio de pin para esta NFC Card ignora este mensaje</p>
    {% elseif action == 'disable' %}
        <p>Desactiva una de estas cards:</p>
        <ul>
            {% for card in cards %}
                <li><a href="{{ url }}{{ card.confirmationToken }}">{{ card.alias }}</a></li>
            {% endfor %}
        </ul>
    {% endif %}

</article>
<footer>
    <a href="https://chip-chap.com/">www.chip-chap.com</a>
</footer>
</body>
</html>
