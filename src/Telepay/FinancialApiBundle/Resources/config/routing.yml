
Admin:
    resource: routing/admin.yml

Manager:
    resource: routing/manager.yml

Manager:
    resource: routing/user.yml

Services:
    resource: routing/incoming.yml

Analytics:
    resource: routing/analytics.yml

Notifications:
    resource: routing/notifications.yml

Wallet:
    resource: routing/wallet.yml
