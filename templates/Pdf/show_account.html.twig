{% block show_account %}
    <strong>{{ "Name" | trans({}, "messages", account.kycManager.locale) }}:</strong>
    <em>{{ account.name }}</em><br/>
    <strong>{{ "Phone" | trans({}, "messages", account.kycManager.locale) }}:</strong>
    <em>+{{ account.prefix }}
        {{ account.phone }}</em><br/>
    {% if account.email %}
        <strong>{{ "Email" | trans({}, "messages", account.kycManager.locale) }}:</strong>
        <em>{{ account.email }}</em><br/>
    {% endif %}
    {% if (account.street != null) and (account.addressNumber != null) %}
        <strong>{{ "Address" | trans }}:</strong>
        <em>
            {{ account.streetType | capitalize }}
            {{ account.street | capitalize}}
            {{ account.addressNumber }},
            {{ account.city | capitalize}}
            {{ account.zip }}
            {% if account.neighbourhood and account.neighbourhood.name != null %}
                ({{ account.neighbourhood.name }})
            {% endif %}
        </em>
    {% endif %}
{% endblock %}
